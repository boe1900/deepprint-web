# DeepPrint Web

一个现代化的、基于浏览器的 Typst 编辑器和预览器，专为高性能票据和文档生成而构建。

![Features](https://img.shields.io/badge/Status-Active-success) ![License](https://img.shields.io/badge/License-MIT-blue)

## 🏗 技术栈

本项目利用最新的 Web 技术提供流畅的本地优先体验：

### 核心与框架
- **React 19**: 利用最新的 React 特性进行高效的 UI 渲染。
- **Vite 7**: 下一代前端工具，支持即时热更新 (HMR) 和优化构建。
- **Hono**: 运行在边缘端的超快 Web 框架，用于后端逻辑。

### 样式与 UI
- **Tailwind CSS v4**: 实用优先的 CSS 框架，支持快速响应式设计。
- **Lucide React**: 美观、一致的图标库。

### 编辑与渲染
- **Monaco Editor**: 提供类似 VS Code 的语法高亮编辑体验。
- **Typst.ts (WASM)**: 通过 WebAssembly 在浏览器中运行完整的 Typst 编译器，实现毫秒级预览更新。
- **Typst.react**: Typst 制品的 React 绑定。

### AI 与智能
- **Vercel AI SDK**: 构建 AI 驱动功能的统一接口。
- **OpenAI**: 驱动 "DeepPrint Copilot"，实现自然语言生成 Typst 代码。

### 基础设施
- **Cloudflare Pages**: 托管静态资源。
- **Cloudflare Functions**: 无服务器后端执行。

## ✨ 主要功能

- **实时预览**: 输入 Typst 代码时即时反馈。
- **基于 WASM 的编译**: 零延迟编译，完全在客户端运行。
- **AI Copilot**: 使用自然语言生成布局和模板。
- **JSON 数据注入**: 逻辑与数据分离，支持动态文档生成。
- **主题支持**: 内置浅色、深色及跟随系统主题模式。

## 🚀 开发指南

### 前置要求

- Node.js 18+
- npm

### 快速开始

1.  **安装依赖**:
    ```bash
    npm install
    ```

2.  **启动开发服务器**:
    ```bash
    npm run dev
    ```
    此命令会自动同步本地 Typst universe 包并启动 Vite。

## ☁️ Cloudflare 部署

本项目已配置为在 Cloudflare Pages 上部署。

### 部署步骤

1.  **登录 Cloudflare**:
    ```bash
    npx wrangler login
    ```

2.  **部署**:
    ```bash
    npm run deploy
    ```

3.  **环境变量**:
    部署后，请记得在 Cloudflare Pages 仪表板的 **Settings** -> **Environment Variables** 下设置 `OPENAI_API_KEY`。这是 AI 功能所必需的。

4.  **本地预览**:
    要在本地使用 Cloudflare Functions 预览应用程序：
    ```bash
    npm run preview
    ```
